# Frontend (Phase 1 MVP)

Run overview dashboard for CCTS Phase 1 prediction experiments.

## Development

- Install dependencies: `npm install`
- Start dev server: `npm run dev`
- Set backend URL via `VITE_API_BASE_URL` (default: `http://127.0.0.1:8000`)
